---
# Frontend Services (NodePort)
apiVersion: v1
kind: Service
metadata:
  name: todo-svc-frontend
  labels:
    app: todo-frontend

spec:
  type: ClusterIP
  selector:
    app: todo-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80